name: 4.3.Versions_Python

# Définition des jobs
jobs:
  version_python:
    name: Version de Python
    runs-on: ubuntu-latest
    strategy:
      matrix:
        version: ["3.8", "3.9", "3.10"]
    steps:
    - name: Installer Python
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.version }}
    - name: Afficher la version de Python
      run: python --version